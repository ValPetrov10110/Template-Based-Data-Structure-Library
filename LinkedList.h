#pragma once
#include <iostream>

template <typename T>
class LinkedList {
private:
    struct Node {
        T data;
        Node* next;
    };

public:

    // The constructor for the Linked List
    LinkedList() {

    }

    // The destructor for the Linked List
    ~LinkedList() {

    }

    // Add an element to the beginning of the list
    void addFirst(const T& a) {

    }

    // Add an element to the end of the list
    void addLast(const T& a) {

    }

    // Add an element at a specified position
    void add(int index, T a) {

    }

    // Removes the element in the beginning
    void removeFirst() {

    }

    // Removes the element at the end
    void removeLast() {

    }

    // Removes the first occurence of the specified element
    void remove_Object(T a) {

    }

    // Remove the element at the given index
    void remove_index(int index) {

    }

    // Removes all elements from the list
    void clear() {

    }

    // Returns element at the beginning of the list
    T& getFirst() {

    }

    // Returns the element at the end of the list
    T& getLast() {

    }

    // Returns the size of the Linked List
    size_t size() {

    }

    // Returns true/false is the Linked List is empty
    bool isEmpty() {

    }



};